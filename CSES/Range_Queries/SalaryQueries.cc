#include <cstdint>
#include <iostream>

using i64 = int64_t;
using u64 = uint64_t;
using u32 = uint32_t;
using u128 = unsigned __int128;

void qsort(int **, size_t, size_t);
inline void swap(int **, size_t, size_t);
inline int get(int *, size_t);
inline void add(int *, size_t, int);

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  return 0;
}
