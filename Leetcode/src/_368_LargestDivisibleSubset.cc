#include <vector>

class Solution
{
public:
  std::vector<int> largestDivisibleSubset(std::vector<int> &nums)
  {
    int n = static_cast<int>(nums.size());

    int size[n]; // largest subset from [0..i] satisfying conditions
  }
};
